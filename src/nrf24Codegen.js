const clean = (v) => String(v ?? "").replace(/[\r\n]+/g, " ").replace(/\*\//g, "* /");
const rxAddr = (id) => `EL${String(id).padStart(3, "0")}`;

export function buildNrf24ReceiverSketch({ receiverId, costumeName, parts = [] }) {
  const id = Math.max(1, Math.min(8, Number(receiverId) || 1));
  const normalized = (parts.length ? parts : [{ name: "EL", pin: 4, frames: [{ t: 0, on: false }], endMs: 0 }]).map((p, i) => ({
    name: clean(p.name || `PART ${i + 1}`),
    pin: Number.isFinite(Number(p.pin)) ? Number(p.pin) : 4,
    frames: Array.isArray(p.frames) && p.frames.length ? p.frames : [{ t: 0, on: false }],
    endMs: Math.max(0, Number(p.endMs) || 0),
  }));
  const endMs = normalized.reduce((m, p) => Math.max(m, p.endMs), 0);
  const frameBlocks = normalized.map((p, i) => {
    const rows = p.frames.map((f) => `  {${Math.max(0, Math.round(Number(f.t) || 0))}UL, ${f.on ? 1 : 0}}`).join(",\n");
    return `// PART ${i + 1}: ${p.name} / D${p.pin}\nconst Frame PART_${i}_FRAMES[] PROGMEM = {\n${rows}\n};\nconst uint16_t PART_${i}_COUNT = sizeof(PART_${i}_FRAMES) / sizeof(PART_${i}_FRAMES[0]);`;
  }).join("\n\n");
  const runtime = normalized.map((p, i) => `  {${p.pin}, PART_${i}_FRAMES, PART_${i}_COUNT, 0}`).join(",\n");

  return `/* nRF24 EL Stage Receiver RX${id} — ${clean(costumeName || `의상 ${id}`)}\n * UNO + YL-105 + nRF24L01+PA+LNA\n * CE D9 / CSN D10 / MOSI D11 / MISO D12 / SCK D13\n * RF: CH90 / 250kbps / PA_HIGH\n * LINK address: ${rxAddr(id)} / Broadcast: ELCMD\n * Relay outputs are ACTIVE LOW by default.\n */\n#include <SPI.h>\n#include <RF24.h>\n#include <avr/pgmspace.h>\n\nRF24 radio(9, 10);\n#define RECEIVER_ID ${id}\n#define RELAY_ACTIVE_LOW 1\n#define END_MS ${Math.max(0, Math.round(endMs))}UL\n\nconst byte UNIQUE_ADDRESS[6] = "${rxAddr(id)}";\nconst byte BROADCAST_ADDRESS[6] = "ELCMD";\nconst byte MAGIC = 0xA5;\nconst byte CMD_PING = 1, CMD_SYNC = 2, CMD_START = 3, CMD_STOP = 4;\n\nstruct __attribute__((packed)) RadioPacket {\n  byte magic; byte type; byte target; uint16_t seq;\n  uint32_t masterTimeMs; uint32_t executeAtMasterMs;\n};\nstruct Frame { uint32_t t; byte on; };\n\n${frameBlocks}\n\nstruct PartRuntime { byte pin; const Frame* frames; uint16_t frameCount; uint16_t nextIndex; };\nPartRuntime PARTS[] = {\n${runtime}\n};\nconst byte PART_COUNT = sizeof(PARTS) / sizeof(PARTS[0]);\n\nint32_t masterOffsetMs = 0;\nbool clockSynced = false;\nbool playing = false;\nuint16_t activeCueSeq = 0;\nuint32_t playbackStartMasterMs = 0;\n\nvoid writePart(byte pin, bool on) {\n#if RELAY_ACTIVE_LOW\n  digitalWrite(pin, on ? LOW : HIGH);\n#else\n  digitalWrite(pin, on ? HIGH : LOW);\n#endif\n}\nvoid allOff() { for (byte i=0; i<PART_COUNT; i++) writePart(PARTS[i].pin, false); }\nvoid resetTimeline() { for (byte i=0; i<PART_COUNT; i++) PARTS[i].nextIndex = 0; allOff(); }\nvoid syncClock(uint32_t masterMs) { masterOffsetMs = (int32_t)(masterMs - millis()); clockSynced = true; }\nuint32_t masterNow() { return (uint32_t)((int32_t)millis() + masterOffsetMs); }\nuint32_t frameTime(const PartRuntime& p, uint16_t i) { return pgm_read_dword(&p.frames[i].t); }\nbool frameOn(const PartRuntime& p, uint16_t i) { return pgm_read_byte(&p.frames[i].on) != 0; }\nvoid updateTimeline(uint32_t elapsed) {\n  for (byte n=0; n<PART_COUNT; n++) {\n    PartRuntime& p = PARTS[n];\n    while (p.nextIndex < p.frameCount && frameTime(p, p.nextIndex) <= elapsed) {\n      writePart(p.pin, frameOn(p, p.nextIndex));\n      p.nextIndex++;\n    }\n  }\n}\nvoid stopPlayback() { playing = false; allOff(); }\n\nvoid setup() {\n  for (byte i=0; i<PART_COUNT; i++) pinMode(PARTS[i].pin, OUTPUT);\n  allOff();\n  if (!radio.begin()) while (1) { allOff(); delay(500); }\n  radio.setDataRate(RF24_250KBPS);\n  radio.setChannel(90);\n  radio.setPALevel(RF24_PA_HIGH);\n  radio.setCRCLength(RF24_CRC_16);\n  radio.setAddressWidth(5);\n  radio.openReadingPipe(0, UNIQUE_ADDRESS);\n  radio.openReadingPipe(1, BROADCAST_ADDRESS);\n  radio.setAutoAck(0, true);   // individual LINK ping\n  radio.setAutoAck(1, false);  // broadcast: prevent ACK collision\n  radio.startListening();\n}\n\nvoid loop() {\n  byte pipe = 0;\n  while (radio.available(&pipe)) {\n    RadioPacket p; radio.read(&p, sizeof(p));\n    if (p.magic != MAGIC) continue;\n    if (p.target != 0 && p.target != RECEIVER_ID) continue;\n    if (p.type == CMD_PING) continue;\n    if (p.type == CMD_SYNC) { syncClock(p.masterTimeMs); continue; }\n    if (p.type == CMD_STOP) { syncClock(p.masterTimeMs); stopPlayback(); continue; }\n    if (p.type == CMD_START) {\n      syncClock(p.masterTimeMs);\n      if (!playing || p.seq != activeCueSeq) {\n        activeCueSeq = p.seq;\n        playbackStartMasterMs = p.executeAtMasterMs;\n        resetTimeline();\n        playing = true;\n      }\n    }\n  }\n  if (!playing || !clockSynced) return;\n  const uint32_t now = masterNow();\n  if ((int32_t)(now - playbackStartMasterMs) < 0) return;\n  const uint32_t elapsed = now - playbackStartMasterMs;\n  if (elapsed >= END_MS) { stopPlayback(); return; }\n  updateTimeline(elapsed);\n}\n`;
}

export function buildNrf24MasterSketch({ receiverCount = 8 } = {}) {
  const count = Math.max(1, Math.min(8, Number(receiverCount) || 8));
  return `/* nRF24 EL Stage MASTER\n * UNO + nRF24L01+PA+LNA + I2C 1602 LCD\n * nRF24: CE D9 / CSN D10 / MOSI D11 / MISO D12 / SCK D13\n * LCD: SDA A4 / SCL A5 / 5V / GND\n * ENABLE rocker: D2-GND, START rocker: D3-GND\n * LCD address default 0x27 (change to 0x3F if needed).\n */\n#include <SPI.h>\n#include <RF24.h>\n#include <Wire.h>\n#include <LiquidCrystal_I2C.h>\n\nRF24 radio(9,10);\nLiquidCrystal_I2C lcd(0x27,16,2);\n#define ENABLE_PIN 2\n#define START_PIN 3\n#define RECEIVER_COUNT ${count}\n#define START_LEAD_MS 300UL\n#define SYNC_INTERVAL_MS 100UL\n#define LINK_SCAN_INTERVAL_MS 120UL\n#define LINK_FAIL_MS 1800UL\n\nconst byte BROADCAST_ADDRESS[6] = "ELCMD";\nconst byte RECEIVER_ADDRESSES[8][6] = {\n  "EL001","EL002","EL003","EL004","EL005","EL006","EL007","EL008"\n};\nconst byte MAGIC = 0xA5;\nconst byte CMD_PING = 1, CMD_SYNC = 2, CMD_START = 3, CMD_STOP = 4;\nstruct __attribute__((packed)) RadioPacket {\n  byte magic; byte type; byte target; uint16_t seq;\n  uint32_t masterTimeMs; uint32_t executeAtMasterMs;\n};\n\nbool linkOk[8] = {0};\nuint32_t lastLinkOkMs[8] = {0};\nbyte scanIndex = 0;\nuint16_t cueSeq = 0;\nuint32_t lastSyncMs = 0, lastScanMs = 0;\nbool lastEnable = false, lastStart = false;\n\nRadioPacket packet(byte type, byte target=0) {\n  RadioPacket p = {}; p.magic=MAGIC; p.type=type; p.target=target; p.seq=cueSeq; p.masterTimeMs=millis(); return p;\n}\nvoid broadcast(byte type, uint32_t executeAt=0, byte repeats=1) {\n  radio.stopListening(); radio.setAutoAck(false); radio.openWritingPipe(BROADCAST_ADDRESS);\n  RadioPacket p = packet(type,0); p.executeAtMasterMs=executeAt;\n  for (byte i=0;i<repeats;i++) { radio.write(&p,sizeof(p)); if(repeats>1) delay(3); }\n}\nvoid sendSync() { broadcast(CMD_SYNC); }\nvoid sendStart() { cueSeq++; broadcast(CMD_START, millis()+START_LEAD_MS, 5); }\nvoid sendStop() { cueSeq++; broadcast(CMD_STOP, 0, 5); }\nvoid pingOne(byte i) {\n  radio.stopListening(); radio.setAutoAck(true); radio.setRetries(3,5); radio.openWritingPipe(RECEIVER_ADDRESSES[i]);\n  RadioPacket p = packet(CMD_PING, i+1);\n  const bool ok = radio.write(&p,sizeof(p));\n  if(ok) { linkOk[i]=true; lastLinkOkMs[i]=millis(); }\n  else if(millis()-lastLinkOkMs[i] > LINK_FAIL_MS) linkOk[i]=false;\n}\nvoid drawLcd() {\n  lcd.setCursor(0,0); lcd.print("12345678 LINK   ");\n  lcd.setCursor(0,1); byte ok=0;\n  for(byte i=0;i<8;i++){ if(i<RECEIVER_COUNT && linkOk[i]){lcd.print('O');ok++;} else if(i<RECEIVER_COUNT) lcd.print('X'); else lcd.print('-'); }\n  lcd.print(' '); lcd.print(ok); lcd.print('/'); lcd.print(RECEIVER_COUNT); lcd.print("   ");\n}\nvoid setup(){\n  pinMode(ENABLE_PIN,INPUT_PULLUP); pinMode(START_PIN,INPUT_PULLUP);\n  lcd.init(); lcd.backlight(); lcd.print("EL STAGE BOOT");\n  if(!radio.begin()){ lcd.setCursor(0,1); lcd.print("NRF24 FAIL"); while(1) delay(500); }\n  radio.setDataRate(RF24_250KBPS); radio.setChannel(90); radio.setPALevel(RF24_PA_HIGH); radio.setCRCLength(RF24_CRC_16); radio.setAddressWidth(5);\n  lastEnable = digitalRead(ENABLE_PIN)==LOW; lastStart = digitalRead(START_PIN)==LOW; delay(300); lcd.clear();\n}\nvoid loop(){\n  const uint32_t now=millis();\n  if(now-lastSyncMs>=SYNC_INTERVAL_MS){lastSyncMs=now;sendSync();}\n  if(now-lastScanMs>=LINK_SCAN_INTERVAL_MS){lastScanMs=now;pingOne(scanIndex);scanIndex=(scanIndex+1)%RECEIVER_COUNT;drawLcd();}\n  bool enable=digitalRead(ENABLE_PIN)==LOW, start=digitalRead(START_PIN)==LOW;\n  if(enable!=lastEnable){delay(20);bool v=digitalRead(ENABLE_PIN)==LOW;if(v!=lastEnable){lastEnable=v;if(!v)sendStop();}}\n  if(start!=lastStart){delay(20);bool v=digitalRead(START_PIN)==LOW;if(v!=lastStart){lastStart=v;if(v&&lastEnable)sendStart();}}\n}\n`;
}
